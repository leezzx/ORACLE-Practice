-- VIEW 생성

CREATE VIEW NOTICE_VIEW
AS
SELECT N.ID, N.TITLE, N.WRITER_ID, M.NAME WRITER_NAME, COUNT(N.ID) COMMENT_CNT
FROM MEMBER M 
RIGHT OUTER JOIN NOTICE N
ON M.ID = N.WRITER_ID
LEFT OUTER JOIN "COMMENT" C
ON N.ID = C.NOTICE_ID
GROUP BY N.ID, N.TITLE, N.WRITER_ID, M.NAME;



-- DATA DICTIONARY
SELECT * FROM DICT;

SELECT * FROM USER_TABLES; -- 만든 테이블 보기
SELECT * FROM USER_TAB_COLUMNS WHERE TABLE_NAME = 'MEMBER'; -- 만든 컬럼 보기